Not required for normal use — every icon on the site comes from the Font
Awesome CDN (see the <link> tag in each page's <head>). Use this folder only
if you want to add a custom local SVG icon somewhere.
